apiVersion: v1
kind: Service
metadata:
  name: saf3ai-agent
  labels:
    app.kubernetes.io/name: saf3ai-agent
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: saf3ai-agent
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
